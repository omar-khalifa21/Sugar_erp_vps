# Copy to /opt/sugar-erp/.env and fill in manually. Never commit the filled file.
# For an existing database volume, keep its original database name, user and password.
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
# Use the Docker host name postgres and port 5432; URL-encode special password characters.
DATABASE_URL=
JWT_SECRET=
