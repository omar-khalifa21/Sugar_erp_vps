# Kitchen installer

Run `./build-installer.ps1 -Version 0.1.0` on Windows. The self-contained installer contains no site identity, enrollment secret, or SQLite data. Operational data remains under `%LOCALAPPDATA%\Sugar ERP\Kitchen` across upgrades.
